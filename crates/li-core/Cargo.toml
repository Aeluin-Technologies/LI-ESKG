[package]
name = "li-core"
version = "0.2.0"
edition = "2024"

[dependencies]
bytes = { version = "1.10", features = ["serde"] }
serde = { version = "1", features = ["derive", "rc"] }
chrono = "0.4"
thiserror = "2"

[dev-dependencies]
serde_json = "1"
